image: busybox

pages:
  stage: deploy
  script:
    - apt install python3
    - scripts/deploy.sh
    - echo "The site will be deployed to $CI_PAGES_URL"
  artifacts:
    paths:
      - public
  only:
    - main
