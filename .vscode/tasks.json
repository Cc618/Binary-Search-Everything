{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_cpp",
            "type": "shell",
            "command": "g++ `pkg-config -cflags -libs gtest` -o /tmp/exo -g --std=c++17 ${file}",
            "options": {
                "cwd": "${workspaceFolder}"
            },
        }
    ]
}